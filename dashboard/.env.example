# Optional: override API URL (used at build time if set).
# For local dev with API on port 3001: VITE_API_URL=http://localhost:3001
# For deployed nip.io: leave unset; config.json or hostname (api.*) is used.
# VITE_API_URL=http://api.127.0.0.1.nip.io
